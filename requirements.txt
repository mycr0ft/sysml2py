# Runtime dependencies (canonical source: pyproject.toml — Poetry-managed).
# Keep in sync with [tool.poetry.dependencies]; extras not listed here.
pint>=0.24
PyYAML>=6.0
antlr4-python3-runtime>=4.13
arrow>=1.4.0
